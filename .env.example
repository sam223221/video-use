ELEVENLABS_API_KEY=

# Only needed for the video-use Studio DOCKER run path (studio/docker-compose.yml),
# where the embedded Claude Agent SDK bills pay-as-you-go. Leave EMPTY for the
# native launcher (studio/start.bat) so the agent uses your Claude MAX login.
ANTHROPIC_API_KEY=