"""Studio core services (state, jobs, uploads, events, persistence).

SCAFFOLD: empty placeholder package. The Backend Engineer implements the
modules described in DOCUMENT.md / ARCHITECTURE.md §3. Importing this package
must stay side-effect free until those land, so the app keeps booting.
"""
