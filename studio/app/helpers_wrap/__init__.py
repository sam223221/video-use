"""Helper-wrapper layer: the single integration seam with ../helpers/.

SCAFFOLD: empty placeholder package. The Backend Engineer implements the
subprocess runners and parsers described in DOCUMENT.md / ARCHITECTURE.md §2.3
and §3. Nothing here imports the agent; both the agent's tools and plain REST
endpoints call into this layer so the wrapper logic exists exactly once.
"""
