"""HTTP routers. Each module is one APIRouter mounted by app.main.create_app().

SCAFFOLD: empty placeholder package. The Backend Engineer implements the routers
described in DOCUMENT.md / ARCHITECTURE.md §3 and API_CONTRACT.md. Routers call
into core/, helpers_wrap/, and agent/ — they never shell out or import helper
modules directly (ARCHITECTURE.md §2.1).
"""
